# AGENTS.md

Agentic-RAG-Search-Assistant = AI 云盘。三个服务由 `docker-compose.yml` 串联（backend → agent 代理转发，frontend → backend `/api/v1`）。代码注释与文档均为中文，新增内容请保持同风格。**Go 后端已删除，Java 是唯一后端**（backend 无 `.go` 文件）。Git 可用但历史很短（曾被重写），别指望从旧提交找线索。

## 目录结构
- `backend/` — Spring Boot 3.3 + Java 21 + Maven + MySQL/Redis/MinIO。入口 `com.clouddrive.CloudDriveApplication`（`src/main/java/com/clouddrive/`）。分层：`controller`（REST）→ `service` → `repository`（Spring Data JPA）→ `entity`；另有 `proxy/`（`AgentClient.java` 把全部 `/api/v1/agent/*` 代理给 Python agent，SSE 透传）、`storage/`（`MinioStorage.java`，AWS SDK v2 S3Client）、`security/`（JWT+双认证+agent token 轮换）、`common/`（Resp 信封/错误码）、`config/`（`AppProperties` + `CD_` 覆盖）。
- `agent/` — Python 3.12 + FastAPI + **Agent 编排框架（无 langgraph 依赖，只保留 langchain-openai/langchain-core）**。入口 `app/main.py`；路由在 `app/router/`（`chat`/`index`/`memory`/`summary`）。完整回合编排在 `app/agent/workflow.py` 的 `AgentWorkflow.turn()`（单 async generator：历史→记忆→技能→上下文折叠→supervisor 路由→worker ReAct 循环→持久化→后台记忆提炼→meta/done），worker 已给出文本答复后 supervisor 收尾去重（`workflow.py` `_collect` 抑制复述、不入历史，避免重复输出），工具/记忆/技能/上下文分别由 `app/agent/` 下的 `tool_manager.py` / `memory_manager.py` / `skill_manager.py` / `context_manager.py` 承担；worker 划分（`file`/`web`/`general`，记忆工具并入 general）见 `app/agent/workers.py`。**全部提示词统一在 `app/prompt/prompts.py`**（按域分节：`SUPERVISOR_SYSTEM_PROMPT`/`WORKER_PROMPTS`/`SUMMARIZE_PROMPT`/`DEDUP_PROMPT`/`SKILL_CONTEXT_GUARD` 等，`app/prompt/__init__.py` 聚合导出）。系统提示词统一四段式（角色设定/能力边界/回复规范/安全约束），**不在提示词里手写枚举工具**——工具名由 `build_worker_prompt`/`build_supervisor_prompt` 运行时从 `worker_tools()`/`WORKER_NAMES` 自动注入（防漂移），工具语义由 `bind_tools` schema 承担（转移工具描述含 `WORKER_TAGLINES`）。语义检索 = Qdrant `kb` collection 中的稠密+稀疏向量 RRF 融合，再经 Cross-Encoder 精排和双门控（`app/core/vector_store.py`/`embedding.py`），`user_id` payload 过滤隔离，记忆 = Redis + SQLite 双写，LLM 走 OpenAI 兼容接口。
- `agent/` 多格式索引：文本提取走 `app/core/rag.py` 的 `_EXTRACTORS` 注册表（扩展名 → 语义族+提取器）。txt 直接解码、pdf 用 PyMuPDF、docx 用 python-docx；**xlsx/xls/pptx/ppt/csv/html 和 jpg/jpeg/png 图片用 MarkItDown**（`partial(_extract_markitdown, ext=...)`，产出 Markdown 文本进现有向量链路）。图片提取会读取请求级 `llm_override`（index 路由的 `_with_llm_override` 从 `X-LLM-*` 头设置）构建 OpenAI 视觉 client 生成中文描述（anthropic/未配 key 时跳过）；`extract_text` 在 `_index_one` 里走 `asyncio.to_thread` 防阻塞事件循环。**MarkItDown 只用 `convert_stream`（内存字节），绝不 `convert()`/`convert_uri()`**（README 警告 `convert()` 可抓取 URL = SSRF 面）。gif/webp/bmp 保持跳过（MarkItDown 图片转换器仅接受 jpg/jpeg/png）。
- `agent/` 多轮会话缓存：**worker/supervisor system prompt 不注入当前时间**（`prompts.py` `build_worker_prompt` 已移除），当前时间由 `workflow.turn` 附加到本轮 user 消息末尾——保证 system + 历史前缀逐字稳定，命中 provider 自动上下文缓存（DeepSeek 默认开启）。缓存感知计费在 `llm_utils._record_usage_meta`（`prompt_tokens_details.cached_tokens` 拆分，`model_meta.py` 每模型配 `price_in_cached`），meta 事件透传 `prompt_cache_hit_tokens`。长会话折叠 `context_budget.py` 支持 `CONTEXT_KEEP_TURNS`（默认 10，0=纯 token 模式）：预算内优先保留最近 N 轮完整消息（A20 起不再无条件越预算）、更早折叠进滚动摘要。
- `agent/` 技能体系：全局技能 `agent/skills/` 支持两种形态——目录式 `<name>/SKILL.md`（可带 `tools.py`，其 `@tool`/`@safe_tool` 注册为可执行工具，admin 可信）+ 扁平式 `<name>.md`（纯指令式，无代码）；云盘技能 = 用户网盘根目录 `skills/` 里的 `.md`，纯指令式，**永不执行用户代码**。加载/触发/热更新见 `app/core/skills/`（`_template`、下划线前缀均为模板不加载）。
- `agent/` 后台任务统一骨架 `app/core/bg_tasks.py`：进程内「尽力而为」任务复用 `run_bg`（fire-and-forget + 指数退避重试，最终失败仅日志）或 `await_with_retry`（同步等待重试）。已迁移：记忆提炼（`memory_manager.schedule_extraction`）、会话消息持久化（`workflow.py` 收尾 `replace_messages`）。新增此类异步收尾逻辑请走该骨架，勿再裸 `create_task`/`except 后仅日志`。
- `frontend/` — React 18 + TS + Vite + Tailwind。常规请求统一经 `src/api/`（`client.ts` 自动注入 `localStorage.token` 作为 Bearer；SSE 流式用裸 `fetch` 手写头）。Vite dev 代理 `/api` → `localhost:8080`；**公开分享 `/s/:token` 无 JWT，`api/shares.ts` 用 `VITE_BACKEND_ORIGIN`（默认 `http://localhost:8080`）直连后端绕过代理**。docker 部署由 nginx 服务（compose 端口 3000）。

## 命令（工作目录很重要）
- 基础设施：`docker compose up -d mysql redis minio qdrant`。本地 MinIO API 端口是 `localhost:9100`（9000 被映射走了），不是默认端口——`application-dev.yml` 已指向它。
- Backend（在 `backend/` 下）：`mvn spring-boot:run`（dev，默认 127.0.0.1:8080）；验证 `mvn test`（JUnit + Mockito 纯单测，不启 Spring 上下文，无需基础设施；单个用例 `mvn test -Dtest=FileServiceTest`）；打包 `mvn package` 后 `java -jar target/backend-0.1.0.jar` 启动。健康检查轮询 `/health`。
- OpenAPI（在 `backend/` 下）：springdoc 自动生成（无 swag），dev 下 `/swagger-ui` 可见。新增 agent 接口注册到 `controller/AgentController.java`，与 Python 侧 `agent/router/` 的路由一一对应。
- Agent（在 `agent/` 下）：先 `copy .env.example .env` 填 LLM 密钥/`REDIS_URL`/`BACKEND_URL`（`config.py` 加载它，`BACKEND_URL` 以 `/api/v1` 结尾；`QDRANT_URL` 不在 example 里，缺省即 `http://localhost:6333`；LLM 密钥会被后端按请求注入的 `X-LLM-*` 头覆盖，env 只是 fallback，见 `core/llm_override.py` contextvar）。然后 `pip install -r requirements.txt`（含 markitdown 多格式提取）；运行 `uvicorn app.main:app --reload --port 8000`。单测：`pytest test/`（全部 mock，无需基础设施）。`test/test_memory.py`、`test/test_search.py`、`test/test_chain.py` 是脚本式（用 `python` 运行，不是 pytest）：test_memory 需真实 Redis（SQLite 走临时库）、test_search 需真实 Qdrant + 本机 embedding 模型、test_chain 需后端 8080 + agent + LLM 密钥的完整链路。RAG 评估：`python -u test/eval_rag.py --top-k 5` 默认只评估召回阶段；加 `--with-rerank` 会额外运行 Cross-Encoder 终排，耗时明显增加（需 Qdrant；结果写入 gitignored 的 `data/eval_results.json`）。agent 自身暴露 `/health`、`/metrics`。
- Frontend（在 `frontend/` 下）：`npm install`；`npm run dev`（127.0.0.1:5173）；`npm run build`（= `tsc -b && vite build`，唯一类型检查；无 linter）。

## 配置模型（backend）
`application.yml` + `application-<dev|prod>.yml`，`APP_MODE` 环境变量选 profile；任意字段可用 `CD_<SECTION>_<FIELD>` 覆盖（如 `CD_SERVER_PORT`、`CD_MINIO_USE_SSL`）。prod yaml 使用 docker 服务名（`mysql`、`redis`、`minio:9000`、`agent:8000`）。`llm.encryption-key`（用户 LLM 配置的 AES-GCM 主密钥，≥32 字节）在 prod 必须通过 `CD_LLM_ENCRYPTION_KEY` 提供。

## LLM 配置（按供应商、存于后端）
用户 AI 配置按供应商存在后端（`llm_configs` 表，密钥 AES-GCM 加密），经 `LLMConfigController` 的 `GET/PUT/DELETE /api/v1/llm-config[/:provider]` 访问（`service/LLMConfigService.java`）。前端只发 `X-LLM-Provider`；`proxy/AgentClient.java` 解密已存密钥并注入 `X-LLM-Base-URL/X-LLM-Key/X-LLM-Model`（provider 为 `tavily` 时加 `X-Tavily-Key`）再转发，agent 侧按请求读取（`chat.py`/`summary.py` 的 `llm_override`）。编辑某个 provider 不影响其他 provider；密钥长度无上限。每个 agent 接口必须在 `AgentController.java` 显式注册（后端代理层，非自动转发）。

## 后端（backend/）注意事项
- **工作约定（务必遵守）**：多步骤任务连续推进、不要停在中间等用户输入「继续」——一次性做完并在结尾汇总。启动 Java 后端验证时用**后台异步方式**（`Start-Process java -ArgumentList "-jar",... -RedirectStandardOutput ...` + 轮询 `/health`），不要阻塞等待；每个阶段结束汇报即可。
- 双认证：`Authorization` Bearer JWT + agent 内部 `X-Agent-Token`（`security/JwtService.java`、`security/AuthFilter.java`、`security/AgentTokenManager.java`，token 轮换走 Redis `internal:agent:token`，Python 侧 `app/auth_token.py` 读取同一 key）。
- **调用方角色分离**：`AuthFilter` 通过 agent token 时设 `caller=agent`，JWT 时设 `caller=user`（request attribute）。需 agent-only 的接口在 Controller 校验（如 `PUT /files/{id}/content` 走 `requireAgent`，用户前端直调 403）；`GET /files/{id}/content` 文本行读取对 user/agent 都开放（前端预览复用）。
- **下载端点（F4）**：`GET /files/{id}/download` 返回已知长度 Resource（`KnownLengthInputStreamResource`）支持 Range（视频 206 流式）；`AuthFilter` **仅对 `/download` 路由**允许 `?token=` JWT 查询参数回退（`<video>` 无法带 Authorization 头）——勿扩大到其他路由，否则削弱信任链。分享 `/s/{token}/download` 无鉴权同样支持 Range。
- agent 无入站鉴权：它信任后端代理注入的 `X-User-Id`。切勿直接暴露 agent:8000（docker-compose 仅绑定 `127.0.0.1`）。**B3 已修复**：`AgentController.HOP_BY_HOP` 已含 `x-user-id`/`x-agent-token`/`x-llm-*`/`x-tavily-key`（入站敏感头不透传，`X-LLM-Provider` 由 `injectLLMConfig` 按后端解析结果重新注入，防止「双头取首」击穿信任链）——新增代理接口或改头复制逻辑时保持该写法，勿回退为透传。
- 文件删除（单个 / 文件夹级联）通过 `service/AgentNotifier.java`（`notifyUnindex`）异步通知 agent 从 Qdrant 取消索引（尽力而为）；**文件内容覆盖写（`PUT /files/{id}/content`）走 `notifyReindex` 异步重建索引**，防止检索命中过期内容。**新建文件（上传/秒传/分块 complete）成功后同样 `notifyReindex`（D8）**——新文件自动可检索，>100MB 由 agent 侧跳过。改删除/覆盖写/新建代码时务必保留这些调用，否则索引状态与文件内容不一致。
- **索引通知走 Redis 队列（`task:index_notify`）**：`AgentNotifier` 生产端 `LPUSH` 入队，`@Scheduled` 每 2s 轮询 `RPOP` 消费，失败按指数退避重试（`MAX_ATTEMPTS=3`），超限丢弃并告警；**Redis 入队失败时回退直发** agent:8000（单线程 executor，尽力而为）。队列任务携带 `X-User-Id` + `X-Agent-Token`（agent 的 index 接口仅校验 `X-User-Id`，未收紧 token 校验）。改这些代码时保持「删除/覆盖写必通知、失败可重试、Redis 不可用不丢」语义。D5 已修：回退直发 HttpClient 已补 `.version(HTTP_1_1)`（对比 `AgentClient`）。
- 秒传为 owner 级限定（`FileRepository.findFirstByMd5AndOwnerId`）并把对象复制到新 key（`MinioStorage.copyObject`）；不要重新引入全局按 MD5 复用。
- 上传限制：直传 ≤ 50MB，分块 10MB，总分 10GB（`AppProperties.Upload` / `application-dev.yml`）。
- Agent 工具（`tools.py`）目前分 file/web/general 三组；general 含 `write_file_content`（覆盖写，走 agent-only 的 `PUT /api/v1/files/{id}/content`）、`edit_file_content`（读→唯一匹配替换→覆盖写，多匹配拒绝）与 `create_file`（按内容新建文本文件，走 agent-only 的 `POST /api/v1/files/text`）。`read_file_content` 文本类走 `GET /api/v1/files/{id}/content` 按行读取（支持 offset/limit），非文本回退 MarkItDown 提取。改这些工具时保持「只操作当前用户、owner 校验」边界，勿引入任意命令执行（`run_command` 明确不做）。
- 代码/配置文件（py/js/ts/go/java/c/cc/cpp/h/hpp/css/json/xml 等）在 agent `_EXTRACTORS`、后端 `FileService.TEXT_EXTS`、前端 `preview.ts TEXT_EXT` 三处**都作为文本**：可被语义索引、按行读取、文本预览。新增代码扩展名需三处同步。
- 避坑：① 所有请求/响应 DTO 都要 `@JsonNaming(SnakeCaseStrategy)`（契约是 snake_case JSON）；② 对象存储用 AWS SDK v2 S3Client（path-style 寻址，原生 S3 multipart），MinIO Java SDK 无 multipart API；③ jjwt 拒绝 <32B HS256 密钥，用 SHA-256 派生；④ **Java HttpClient 默认 HTTP/2 会对明文发 h2c upgrade，代理 agent 必须 `.version(HTTP_1_1)`**；⑤ 代理复制入站头要跳过 Java 受限头（expect/connection/host/upgrade/content-length）；⑥ `@JsonNaming` 的 `PREFIX="v1:"` 与 split 后 parts[0]="v1" 判等坑（AES-GCM 密文前缀校验用 VERSION 不带冒号）；⑦ RAG 的 `kb` 已是 `dense`/`sparse` 命名向量 schema，改 collection schema 后必须在 dev 删除重建并全量索引；⑧ 打包前须停掉正在运行的 jar（Windows 锁文件）；⑨ 用 `edit`/`write` 工具改文件，**禁用 PowerShell 的 `Set-Content`/`Get-Content -Raw -replace` 改含中文的源文件**（会损坏 UTF-8/BOM），改完如有 BOM 需剥离。
- `docs/`（均 gitignored）审查台账：`IMPROVEMENTS.md` 描述**当前**代码库（Java 后端），关键 `file:line` 已逐一交叉核实属实。**B1/B2/B3/B5 已修复（含回归测试：`FileServiceTest`/`AgentControllerTest`/`MultipartServiceTest`/`MinioStorageTest`）**；**D1–D9 已修复**（配额原子预占/扣旧大小/分块预检、唯一索引+重名重试、HTTP_1_1、deleteObjects 分页、文件夹孤儿行、新建自动建索引、批量删除，含回归测试）。P0 余 **B4（JWT 默认密钥无启动校验）**、P1 余 **A6、A21、A24、A25、R1–R9、F3、F5、F6、F8–F13** 未修——改 `AppProperties`/`AgentController`/`AgentNotifier` 等前先读台账 Track，勿让改动与其冲突。`BUSINESS-REVIEW.md` 描述已废弃的 Go `master` 分支（org/ACL/权限层在当前代码库不存在），其修复引用均不适用；仅「删除即取消索引、owner 级秒传 CopyObject」与本代码库一致。LaTeX 项目总结也在 `docs/` 下。
